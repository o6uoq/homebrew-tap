2026-04-11 | packaging/ci | Added new tap formula with autobump wiring | Used npm package identity (`paperclipai`) while keeping formula identity (`paperclip`) for user-facing consistency → reuse this mapping pattern for npm packages whose CLI/package/repo names differ
2026-04-17T09:20:27Z | packaging/paperclip | PR #53 smoke test failed on npm --min-release-age gate for coordinated release dependencies | Overrode install args with --min-release-age=0 for paperclip to restore deterministic installs and green CI → move this into a documented tap policy for npm formulas with lockstep internal deps
2026-04-17T09:35:19Z | ci/autobump | Per-formula min-age bypass solved symptom but violated policy intent | Centralized npm min-release-age enforcement in autobump using Homebrew-derived policy + shared preflight helper, and removed formula override → extract helper as shared org action/workflow for every tap repo
